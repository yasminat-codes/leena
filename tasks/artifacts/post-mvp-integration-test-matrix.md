# Post-MVP Integration Test Matrix

Created: 2026-06-04
Owner task: 145
Status: completed release-gate artifact

This matrix covers the day-one high-power integrations with mocked/fake resources only. No automated row requires real Composio credentials, real Apple Calendar mutation, or broad owner-file mutation. Unknown and stale states are expected to fail closed.

| Integration | Happy path | Missing credential | Denied permission | Unknown or stale status | Write-confirmation case | Automated anchors |
|-------------|------------|--------------------|-------------------|-------------------------|-------------------------|-------------------|
| Composio | Fake credential refresh creates a normal HTTP MCP server entry, uses in-memory session headers, exposes namespaced tools, and executes through MCP after confirmation. | Missing fake credential reports `missing_credential` and refresh rejects before MCP session creation. | Confirmation denial coverage remains in MCP execution tests because Composio execution is the MCP path. | Expired Composio metadata returns no tools and blocks execution as unknown/stale metadata. | Composio write tool requests central confirmation before execution. | `test/post-mvp-integration-matrix.test.js`, `test/mcp-integration.test.js`, `test/mcp-tool-execution.test.js`, `test/mcp-permission-gate.test.js` |
| Custom MCP | Mock HTTP and stdio transports connect, list tools, expose namespaced realtime definitions, reconnect cleanly, and disconnect. | Missing or malformed server config is covered by MCP execution/gate tests. | Denied central confirmation prevents `callTool`. | Stale/mismatched tool metadata becomes `unknown` and blocks, including under permissive policies. | Write-risk schemas infer `write` and surface confirmation with integration trust affordance. | `test/post-mvp-integration-matrix.test.js`, `test/mcp-integration.test.js`, `test/mcp-tool-execution.test.js`, `test/mcp-permission-gate.test.js` |
| Mac access | `computer_use_task` with browser target remains normal Computer Use; real Mac target is centrally classified as destructive/control-risk. | Runtime unavailable and unsupported platform states are existing Computer Use and OS-permission coverage. | Denied Screen Recording/Accessibility style states remain not granted and actionable. | Unknown status blocks confirmation UI instead of presenting an allow prompt. | Real Mac control requires confirmation by default. | `test/post-mvp-integration-matrix.test.js`, `test/os-permissions.test.js`, `test/tool-permissions.test.js` |
| Full Disk Access | Fake metadata-only probe returns `granted` without reading private contents. | Not credential-based; missing probe paths return `unknown`. | Fake `EPERM`/`EACCES` probes return `denied` and do not expose contents. | Missing/unexpected probes return `unknown`; broad file reads with unknown status deny access. | Full-disk trusted writes require `granted` plus Trusted Mac Access and trusted write mode. | `test/post-mvp-integration-matrix.test.js`, `test/os-permissions.test.js`, `test/filesystem-tools.test.js`, `test/tool-permissions.test.js` |
| Apple Calendar | Fake TCC row with read-capable full access returns `granted`; no EventKit mutation is used. | Adapter unavailable/platform unsupported remains guided setup in existing adapter tests. | Fake TCC denied row returns `denied`; read-capable denial beats grants in existing tests. | Unreadable TCC probe returns `unknown`; write-only access returns `restricted`, not read-granted. | Apple create/delete permission requests preserve `apple-calendar` source and require confirmation/trust decision. | `test/post-mvp-integration-matrix.test.js`, `test/os-permissions.test.js`, `test/tool-permissions.test.js`, `test/apple-calendar-adapter.test.js` |
| File access | Workspace read and explicit scoped read succeed in a temporary sandbox. | Not credential-based; invalid/missing paths return expected argument/error results in existing file tests. | Broad full-disk read with non-granted status returns `permission_denied` and no content. | Unknown Full Disk Access denies broad read/write. | Writes ignore model-supplied confirmation, require host confirmation, or require Trusted Mac Access plus trusted write mode. | `test/post-mvp-integration-matrix.test.js`, `test/filesystem-tools.test.js`, `test/tool-permissions.test.js` |
| Central permission confirmations | Known low/read actions can be allowed; integration write/destructive actions create confirmation state. | Unknown first-party or integration metadata has no credential bypass. | Denied result is model-visible and does not call the integration. | Unknown or stale metadata becomes a blocked state with only refresh action. | MCP, Composio, Apple Calendar, file writes, and real Mac control all route through central confirmation/trusted-write rules. | `test/post-mvp-integration-matrix.test.js`, `test/tool-permissions.test.js`, `test/mcp-permission-gate.test.js`, `test/mcp-tool-execution.test.js` |

## Notes

- The matrix intentionally points to both the focused cross-contract suite and existing leaf-contract tests. The new suite proves the rows line up; the existing tests keep detailed edge coverage near each helper.
- Composio remains an integration source, not a permission bypass. The automated path creates normal MCP server metadata and exercises the same MCP execution gate.
- Full Disk Access and Apple Calendar are tested through fake probes/TCC rows only. Owner-granted GUI smoke remains manual for task 146 unless that contract is explicitly changed.
- Unknown/stale metadata must stay blocked, not confirmable and not auto-approved.
